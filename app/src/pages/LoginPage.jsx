import LoginCombo from '../Components/LoginSignSec/LoginCompo'

function LoginPage() {
  return (
    <div>
      {window.scrollTo(0, 0)}
      <LoginCombo/>
    </div>
  )
}

export default LoginPage
