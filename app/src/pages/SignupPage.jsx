import SignupCombo from '../Components/LoginSignSec/SignupCombo'

function SignupPage() {
  return (
    <div>
      {window.scrollTo(0, 0)}
      <SignupCombo/>
    </div>
  )
}

export default SignupPage
